import { PetNotFoundExceptionFilterFilter } from './pet-not-found-exception-filter.filter';

describe('PetNotFoundExceptionFilterFilter', () => {
  it('should be defined', () => {
    expect(new PetNotFoundExceptionFilterFilter()).toBeDefined();
  });
});
