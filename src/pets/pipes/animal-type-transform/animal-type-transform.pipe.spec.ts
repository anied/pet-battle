import { AnimalTypeTransformPipe } from './animal-type-transform.pipe';

describe('AnimalTypeTransformPipe', () => {
  it('should be defined', () => {
    expect(new AnimalTypeTransformPipe()).toBeDefined();
  });
});
